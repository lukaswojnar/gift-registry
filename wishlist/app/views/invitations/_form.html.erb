  <div class="row">
    <div class="small-12 columns">
    <%= form_tag "/invitations/create" do %>
      <%= email_field_tag 'email' %>
      <%= hidden_field_tag :list_id, @list.id %>
      <%= submit_tag "Send", { class: 'button' } %>
    <% end %>
  </div>
</div>